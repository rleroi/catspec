{
  "name": "@catspec/cli",
  "version": "0.1.0",
  "description": "CLI for the CaT (Code as Text) spec — validate, diff, and transpile bacon/ specs.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "cat": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "commander": "^13.1.0",
    "gray-matter": "^4.0.3",
    "js-yaml": "^4.1.0"
  },
  "optionalDependencies": {
    "@cursor/sdk": "^1.0.22"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
